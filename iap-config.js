/**
 * RevenueCat API key for native app (iOS/Android).
 * Get your public API key from: app.revenuecat.com → Project → API Keys
 * Use the PUBLIC key (starts with "appl_" for iOS or "goog_" for Android).
 * For both platforms, you can use either key - RevenueCat will use the right one per platform.
 */
window.GS_IAP_CONFIG = {
  revenueCatApiKey: 'test_YulJqeVUTpxvPpMzwvczcFfzqum'  // e.g. 'appl_xxxxxxxxxxxx' or 'goog_xxxxxxxxxxxx'
};
